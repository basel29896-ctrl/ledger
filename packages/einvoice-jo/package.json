{
  "name": "@acct/einvoice-jo",
  "version": "0.0.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "echo lint:einvoice-jo"
  },
  "dependencies": { "@acct/domain": "workspace:*", "@acct/shared": "workspace:*" }
}
